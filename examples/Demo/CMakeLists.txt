cmake_minimum_required(VERSION 3.0)

SET(CMAKE_MODULE_PATH 
	${CMAKE_MODULE_PATH} 
	${CMAKE_CURRENT_SOURCE_DIR}/CMake
	${CMAKE_CURRENT_SOURCE_DIR}/../../CMake
)

INCLUDE(ExampleBuild)

SET(EXAMPLE_SOURCE_FILES
	main.cpp
	CDemo.cpp
	CMainMenu.cpp
)

if (MSVC_LIKE_COMPILER)
  SET(EXAMPLE_SOURCE_FILES
	${EXAMPLE_SOURCE_FILES}
	resscript.rc
  )
endif ()


SET(EXAMPLE_HEADER_FILES	
	resource.h
	CDemo.h
	CMainMenu.h
)

SET(EXAMPLE_INSTALL_FILES
	icon.ico
)

SET(EXAMPLE_INSTALL_DIRS
)

BUILD_WIN32_EXAMPLE("Demo" "${EXAMPLE_SOURCE_FILES}" "${EXAMPLE_HEADER_FILES}" "${EXAMPLE_INSTALL_FILES}" "${EXAMPLE_INSTALL_DIRS}")

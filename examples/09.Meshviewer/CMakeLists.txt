cmake_minimum_required(VERSION 3.0)

SET(CMAKE_MODULE_PATH 
	${CMAKE_MODULE_PATH} 
	${CMAKE_CURRENT_SOURCE_DIR}/CMake
	${CMAKE_CURRENT_SOURCE_DIR}/../../CMake
)

INCLUDE(ExampleBuild)

SET(EXAMPLE_SOURCE_FILES
	main.cpp	
)

if (MSVC_LIKE_COMPILER)
  SET(EXAMPLE_SOURCE_FILES
	${EXAMPLE_SOURCE_FILES}
	9.Meshviewer.rc
  )
endif ()

SET(EXAMPLE_HEADER_FILES	
)

SET(EXAMPLE_INSTALL_FILES
	tutorial.html
	icon.ico	
)

SET(EXAMPLE_INSTALL_DIRS
)

BUILD_EXAMPLE("09.Meshviewer" "${EXAMPLE_SOURCE_FILES}" "${EXAMPLE_HEADER_FILES}" "${EXAMPLE_INSTALL_FILES}" "${EXAMPLE_INSTALL_DIRS}")
